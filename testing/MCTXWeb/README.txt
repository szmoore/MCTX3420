Uncomment the line with:

mctx.debug = true

In mctx.gui.js to prevent browser redirects and to enable debug messages.

Debug messages are printed to the browser console. (Ctrl+Shift+K in Firefox)

Files which are needed to generate stuff for public_html, but that are not
directly displayed go in raw_data_not_for_publication. e.g svg files